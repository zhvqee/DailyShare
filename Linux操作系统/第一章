第一章 Linux 操作系统概念

用户态 User Mode  、内核态 Kernel Mode

类Unix操作系统在用户应用程序前把与计算机物理组织相关的底层细节都隐藏起来，当程序想使用硬件资源时，必须向操作系统发出一个请求，然后由内核代表应用程序与相关的硬件打交道。
为了实施这种机制，映入了用户态和内核态

多用户系统
用户和组
进程
类Unix操作系统采用进程/内核模式，每个进程都以为自己是系统中唯一的进程，可以独占操作系统所提供的服务。只要进程发出系统调用，硬件就会把特权模式由用户态变为内核态。
硬链接 、软链接（符号链接）